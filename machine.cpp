#include <iostream>

task e_stop()
{
    while(true)
    {
        if(SensorValue(e_sopBtn) == 1)
        {
            stopAllTasks();    //ends the program andall task main.
        }
        wait1Msec(10);
    }
}


////////////////////////////FAIL METHOD, STATEMENT STATEMENT///////////////////////////////

ifstream in("machinerun.c");
if (!in == true)
{
    final.fail();
}
else 
{
    
}

infile.open(self);
if(infile.fail())
{
    cout<<"Mechanical Failure. Emergency stop deployed. Please reset the system."
}
else{
    while(task main)
}

return 0;